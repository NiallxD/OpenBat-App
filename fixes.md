# OpenBat — fixes & ideas backlog

Pruned 2026-07-02: all previously listed bugs are fixed, and most feature ideas
shipped (palettes, runner-up species, pinch zoom, band persistence, pre/post roll,
isWriting indicator, ear icon, bat empty-state icon, GUANO coordinates, AutoID row
layout, compress-time button move). What remains:

## Features / enhancements

- **iPad landscape default split: Pulse view (left 50%) + Species ID list (right 50%).**
  New default landscape layout specifically for iPad, distinct from the current
  iPhone landscape three-column layout.

## Future / parked idea

- **Transect mode.** A preconfigured session type: load a route/geofile of waypoints
  ahead of time, show a small minimap with distance-to-next-point and on-point
  detection, plus a configurable timer. Bigger feature, not scoped yet — just capturing
  the idea before it's lost.
